var navegador = getBrowser();
var logo = document.getElementById("logoNavegador");

if (navegador === "firefox") {
    logo.src = "firefox.jpeg"; 
} else if (browser === "internet-explorer") {
    logo.src = "google.png"; 
} 